import { createStore } from "vuex";
import WhiteboardModule from "@/store/whiteboard";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: { WhiteboardModule },
});
